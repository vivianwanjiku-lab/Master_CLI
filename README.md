# Master_CLI
Master CLI is a fun, interactive quiz game that runs in your terminal/command prompt. It fetches live trivia questions from an online database (OpenTDB), lets you choose difficulty levels, tracks your score, and saves high scores — all without leaving the command line.
